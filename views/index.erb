<% if session[:message] %>
  <p><%= session.delete(:message) %></p>
<% end %>

<ul>
  <% @files.each do |file_name| %>
    <li><a href= "/documents/<%= file_name %>"><%= file_name %></a></li>
  <% end %>
</ul>